apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: cloud-computing
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
  selector:
    app: backend 